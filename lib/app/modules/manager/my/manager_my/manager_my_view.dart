import 'package:flutter/material.dart';
import 'package:get/get.dart';

import 'manager_my_logic.dart';

class ManagerMyPage extends StatelessWidget {
  const ManagerMyPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // final logic = Get.find<ManagerMyLogic>();
    // final state = Get.find<ManagerMyLogic>().state;

    return Container();
  }
}
